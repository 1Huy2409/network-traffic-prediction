# Prediction Service Requirements
# Chạy trên local machine (không trong Docker)

# FastAPI + ASGI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Prediction dependencies (from main requirements.txt)
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
